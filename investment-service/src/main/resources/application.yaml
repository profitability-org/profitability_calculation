server:
  port: 8082
spring:
  application:
    name: investment-service
  config:
    import:
      - "optional:classpath:eureka-client.yaml"
      - "optional:classpath:spring-doc.yaml"
  datasource:
    url: jdbc:postgresql://localhost:5432/profitability_investment
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
